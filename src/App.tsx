// App.tsx
import React from 'react';
import viteLogo from '/vite.svg';  // Referencing the file from the root of the project

function App() {
  return (
    <div>
      <h1>Vite + React</h1>
      <img src={viteLogo} alt="Vite logo" />
    </div>
  );
}

export default App;
